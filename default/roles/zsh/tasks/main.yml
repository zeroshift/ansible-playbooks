---

- name: Clone zprezto
  git:
    repo: "https://github.com/zeroshift/prezto.git"
    dest: "~/.zprezto"
    version: '80ed095d8a33bde2db2e1131607f439ee92e6567'
    recursive: yes
    update: yes

- name: Link rc files
  file:
    src: "~/.zprezto/runcoms/{{ item }}"
    dest: "~/.{{ item }}"
    state: link
  loop:
    - zlogin
    - zlogout
    - zpreztorc
    - zprofile
    - zshenv
    - zshrc

# - name: chsh on OSX
#   user:
#     name: "{{ ansible_user_id }}"
#     shell: /bin/zsh
#   when: ansible_distribution == "MacOSX"
#   become: yes
