---

- name: Clone zprezto
  git:
    repo: "https://github.com/zeroshift/prezto.git"
    dest: "~/.zprezto"
    version: 'cf6b802b4bd3fe2b78cf1533884554487afa3763'
    recursive: yes
    update: yes
  ignore_errors: True

- name: Link rc files
  file:
    src: "~/.zprezto/runcoms/{{ item }}"
    dest: "~/.{{ item }}"
    state: link
  loop:
    - zlogin
    - zlogout
    - zpreztorc
    - zprofile
    - zshenv
    - zshrc

# - name: chsh on OSX
#   user:
#     name: "{{ ansible_user_id }}"
#     shell: /bin/zsh
#   when: ansible_distribution == "MacOSX"
#   become: yes
